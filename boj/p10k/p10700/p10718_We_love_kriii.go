package p10700

import (
	"fmt"
	"io"
)

func Solve10718(writer io.Writer) {
	//goland:noinspection SpellCheckingInspection
	const msg = "강한친구 대한육군"
	_, _ = fmt.Fprintln(writer, msg)
	_, _ = fmt.Fprintln(writer, msg)
}
